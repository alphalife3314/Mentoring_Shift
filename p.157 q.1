#include <stdio.h>

int main()
{
    int a=0, b=0, c=0;

    while(c<5)
    {
        while(b<=0)
        {
            printf("0보다 큰 수를 입력 (%d번째): ", c+1);
            scanf("%d", &b);
        }
        a=a+b;
        b=0;
        c++:
    }
    printf(총합: %d \n", a);
    return 0;
}
