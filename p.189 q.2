#include <stdio.h>

int main()
{
	int num1, num2;
	int result;
	
	while(result>0)
	{
	    printf("두 개의 정수를 입력: ");
	    scanf("%d %d", &num1, &num2);
	
	    if(num1>num2)
	    result=num1-num2;
	    else
	    result=num2-num1;
	
	    printf("뺄셈 결과: %d \n", result);
	}
    return 0;
}
